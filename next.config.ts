import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Nothing fancy needed. Sample videos are loaded from the public
  // gtv-videos-bucket over https, which the browser fetches directly.
};

export default nextConfig;
